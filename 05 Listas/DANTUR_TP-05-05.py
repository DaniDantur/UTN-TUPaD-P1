# Ejercicio 5
#
# Analizar el siguiente programa y explicar con tus palabras qué es lo que realiza.
# numeros = [8, 15, 3, 22, 7]
# numeros.remove(max(numeros))
# print(numeros)

# El programa crea una lista llamada "numeros" que contiene cinco números enteros. 
# Luego, utiliza la función "max()" para encontrar el valor máximo en la lista 
# (en este caso, 22) y lo elimina de la lista utilizando el método "remove()". 
# Finalmente, imprime la lista resultante, que contiene los números originales 
# ([8, 15, 3, 7]) sin el número máximo (22).